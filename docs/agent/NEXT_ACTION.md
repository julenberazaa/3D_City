# NEXT ACTION — never lose the thread

CURRENT MILESTONE: WP-00 bootstrap (harness + docs + model lock) → WP-01 architecture freeze
CURRENT WORK PACKAGE: WP-00
CURRENT BLOCKER: none
NEXT SINGLE ACTION: Create harness scripts + baseline + initial package.json; then run upstream research (WP-01) and freeze architecture.
WHY IT IS NEXT: Bootstrap must be complete and the harness smoke-tested before any product work; the architecture cannot freeze before the upstream audit.
ACCEPTANCE CONDITION: `npm run harness` runs the bootstrap gates with a coherent result; docs/agent complete; agents configured.
EVIDENCE TO PRODUCE: harness run-id report under reports/harness/; baseline capture.
